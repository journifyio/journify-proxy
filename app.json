{
    "name": "journify-proxy",
    "hooks": {
        "prebuild": {
            "commands": [
                "gcloud auth configure-docker"
            ]
        },
        "postcreate":{
            "commands": [
                "gcloud beta run domain-mappings create --service $SERVICE_URL --domain proxy.journify-stage.dev"
            ]
        }
    }
}
